[project]
name = "tachicoma"
version = "0.1.0"
description = "Evidence-governed memory registry for coding agents"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
